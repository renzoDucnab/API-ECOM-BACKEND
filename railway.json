{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-dev --ignore-platform-reqs && php artisan key:generate"
  },
  "deploy": {
    "startCommand": "vendor/bin/heroku-php-apache2 public/",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120
  }
}